import { useEffect, useRef, useState, type FormEvent } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import QRCode from 'qrcode';
import { ArrowDownToLine, Copy, ExternalLink, FileText, KeyRound, LockKeyhole, Plus, QrCode, Search, ShieldCheck, Trash2, Upload } from 'lucide-react';
import { apiFetch, apiUrl, ApiError } from '@/api/client';
import { useAuth } from '@/auth/AuthContext';
import { useI18n } from '@/i18n/I18nContext';
import { Button } from '@/components/ui/Button';
import { Modal } from '@/components/ui/Modal';
import { EmptyState } from '@/components/ui/EmptyState';
import { ErrorState } from '@/components/ui/ErrorState';
import { LoadingState } from '@/components/ui/LoadingState';
import { PageHeader } from '@/components/ui/PageHeader';

type VaultFile={id:number;original_name:string;mime_type:string;file_size:number};
type Vault={id:number;title:string;description?:string|null;is_active:boolean;successful_access_count:number;last_accessed_at?:string|null;files_count?:number;files?:VaultFile[];public_path:string};
const emptyForm={title:'',description:'',password:'',password_confirmation:'',is_active:true};
const input='h-11 w-full rounded-xl border border-slate-200 bg-white px-3 text-xs font-bold text-slate-800 outline-none focus:border-teal-500 focus:ring-2 focus:ring-teal-100';

export function ConfidentialFinancialVaultsPage(){
  const {can}=useAuth();const {locale,t}=useI18n();const ar=locale==='ar';const qc=useQueryClient();
  const [selectedId,setSelectedId]=useState<number|null>(null);const [search,setSearch]=useState('');const [formOpen,setFormOpen]=useState(false);const [editing,setEditing]=useState(false);const [form,setForm]=useState(emptyForm);const [error,setError]=useState('');
  const list=useQuery({queryKey:['confidential-financial-vaults'],queryFn:()=>apiFetch<Vault[]>('/confidential-financial-vaults')});
  const detail=useQuery({queryKey:['confidential-financial-vault',selectedId],queryFn:()=>apiFetch<Vault>(`/confidential-financial-vaults/${selectedId}`),enabled:selectedId!==null});
  useEffect(()=>{if(!list.data)return;if(selectedId!==null&&list.data.some(v=>v.id===selectedId))return;setSelectedId(list.data[0]?.id??null)},[list.data,selectedId]);
  const fail=(value:unknown)=>setError(value instanceof ApiError?value.message:(ar?'تعذر تنفيذ الإجراء.':'Action failed.'));
  const refresh=async()=>{await qc.invalidateQueries({queryKey:['confidential-financial-vaults']});if(selectedId)await qc.invalidateQueries({queryKey:['confidential-financial-vault',selectedId]})};
  const save=useMutation({mutationFn:()=>{const body:Record<string,unknown>={title:form.title,description:form.description||null,is_active:form.is_active};if(form.password){body.password=form.password;body.password_confirmation=form.password_confirmation}return apiFetch<Vault>(editing?`/confidential-financial-vaults/${selectedId}`:'/confidential-financial-vaults',{method:editing?'PUT':'POST',body})},onSuccess:async vault=>{setSelectedId(vault.id);setFormOpen(false);setForm(emptyForm);setError('');await refresh()},onError:fail});
  const toggle=useMutation({mutationFn:(active:boolean)=>apiFetch(`/confidential-financial-vaults/${selectedId}`,{method:'PUT',body:{is_active:active}}),onSuccess:async()=>{setError('');await refresh()},onError:fail});
  const upload=useMutation({mutationFn:(files:File[])=>{const body=new FormData();files.forEach(file=>body.append('files[]',file));return apiFetch(`/confidential-financial-vaults/${selectedId}/files`,{method:'POST',body})},onSuccess:async()=>{setError('');await refresh()},onError:fail});
  const remove=useMutation({mutationFn:(id:number)=>apiFetch(`/confidential-financial-vaults/${selectedId}/files/${id}`,{method:'DELETE'}),onSuccess:refresh,onError:fail});
  if(!can('confidential_finance.manage'))return <ErrorState title={t('state.forbidden.title')} message={t('state.forbidden.message')}/>;
  if(list.isLoading)return <LoadingState/>;if(list.isError)return <ErrorState onRetry={()=>list.refetch()}/>;
  const rows=(list.data??[]).filter(v=>`${v.title} ${v.description??''}`.toLowerCase().includes(search.toLowerCase()));const vault=detail.data;
  const create=()=>{setEditing(false);setForm(emptyForm);setFormOpen(true)};
  const edit=()=>{if(!vault)return;setEditing(true);setForm({title:vault.title,description:vault.description??'',password:'',password_confirmation:'',is_active:vault.is_active});setFormOpen(true)};

  return <div className="mx-auto max-w-7xl space-y-4 pb-14">
    <PageHeader title={ar?'خزنة الملفات المالية السرية':'Confidential financial vault'} description={ar?'حماية الملفات المالية بكلمة مرور ورمز QR دائم مع تحكم كامل بالتفعيل.':'Protect financial files with a password and a permanent QR code.'}><Button onClick={create}><Plus className="me-2 h-4 w-4"/>{ar?'خزنة جديدة':'New vault'}</Button></PageHeader>
    <div className="rounded-2xl border border-amber-200 bg-amber-50 p-3 text-xs font-bold leading-6 text-amber-900"><ShieldCheck className="me-2 inline h-4 w-4"/>{ar?'محتوى سري للغاية: لا تشارك كلمة المرور مع أي شخص غير مخول، ولا تظهر كلمة المرور في النظام أو رمز QR.':'Highly confidential: share the password only with authorized recipients. It never appears in the QR code.'}</div>
    {error&&<p className="rounded-xl border border-rose-100 bg-rose-50 p-3 text-xs font-bold text-rose-700">{error}</p>}
    <div className="grid gap-4 lg:grid-cols-[320px_minmax(0,1fr)]"><aside className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><label className="relative block border-b border-slate-100 p-3"><Search className="absolute end-6 top-6 h-4 w-4 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} className={`${input} pe-10`} placeholder={ar?'بحث':'Search'}/></label>{rows.length===0?<div className="p-5"><EmptyState message={ar?'لا توجد خزائن مالية.':'No financial vaults.'}/></div>:<div className="divide-y divide-slate-100">{rows.map(item=><button key={item.id} onClick={()=>setSelectedId(item.id)} className={`w-full p-4 text-start ${selectedId===item.id?'bg-teal-50':'hover:bg-slate-50'}`}><div className="flex items-center justify-between gap-3"><div className="min-w-0"><p className="truncate text-sm font-black text-slate-900">{item.title}</p><p className="mt-1 text-[10px] font-bold text-slate-500">{item.files_count??0} {ar?'ملف':'files'}</p></div><LockKeyhole className={`h-4 w-4 ${item.is_active?'text-emerald-600':'text-slate-300'}`}/></div></button>)}</div>}</aside>
      <main>{selectedId===null?<div className="rounded-2xl border border-dashed border-slate-300 bg-white p-12"><EmptyState message={ar?'أنشئ خزنة مالية للبدء.':'Create a financial vault to begin.'}/></div>:detail.isLoading?<LoadingState/>:!vault||detail.isError?<ErrorState onRetry={()=>detail.refetch()}/>:<VaultWorkspace vault={vault} ar={ar} onEdit={edit} onToggle={()=>toggle.mutate(!vault.is_active)} onUpload={files=>upload.mutate(files)} onRemove={id=>remove.mutate(id)} busy={toggle.isPending||upload.isPending}/>}</main></div>
    <VaultForm open={formOpen} close={()=>{setFormOpen(false);setForm(emptyForm)}} ar={ar} form={form} setForm={setForm} editing={editing} save={()=>save.mutate()} busy={save.isPending}/>
  </div>;
}

function VaultWorkspace({vault,ar,onEdit,onToggle,onUpload,onRemove,busy}:{vault:Vault;ar:boolean;onEdit:()=>void;onToggle:()=>void;onUpload:(files:File[])=>void;onRemove:(id:number)=>void;busy:boolean}){const publicUrl=`${window.location.origin}${vault.public_path}`;return <section className="space-y-4"><article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div><div className="flex items-center gap-2"><LockKeyhole className="h-5 w-5 text-teal-700"/><h2 className="text-lg font-black text-slate-900">{vault.title}</h2><span className={`rounded-full px-2.5 py-1 text-[10px] font-black ${vault.is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-600'}`}>{vault.is_active?(ar?'الرابط فعال':'Link active'):(ar?'الرابط معطل':'Link disabled')}</span></div>{vault.description&&<p className="mt-2 text-xs leading-6 text-slate-600">{vault.description}</p>}<p className="mt-3 text-[10px] font-bold text-slate-500">{vault.successful_access_count} {ar?'عملية فتح ناجحة':'successful unlocks'} · {vault.last_accessed_at?new Date(vault.last_accessed_at).toLocaleString(ar?'ar-PS':'en-GB'):(ar?'لم يُفتح بعد':'Never opened')}</p></div><div className="flex gap-2"><Button size="sm" variant="outline" onClick={onEdit}>{ar?'تعديل وكلمة المرور':'Edit & password'}</Button><Button size="sm" variant="outline" onClick={onToggle} disabled={busy}>{vault.is_active?(ar?'تعطيل QR':'Disable QR'):(ar?'تفعيل QR':'Enable QR')}</Button></div></div></article><div className="grid gap-4 xl:grid-cols-[300px_minmax(0,1fr)]"><PermanentQr value={publicUrl} active={vault.is_active} ar={ar}/><article className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="mb-3 flex items-center justify-between"><div><h3 className="text-sm font-black text-slate-900">{ar?'الملفات السرية':'Confidential files'} ({vault.files?.length??0})</h3><p className="mt-1 text-[10px] font-bold text-slate-500">{ar?'حتى 10 ملفات، 20 م.ب للملف.':'Up to 10 files, 20 MB each.'}</p></div><label className="inline-flex h-9 cursor-pointer items-center rounded-lg bg-teal-700 px-3 text-xs font-bold text-white hover:bg-teal-800"><Upload className="me-2 h-4 w-4"/>{ar?'رفع ملف':'Upload'}<input type="file" multiple className="sr-only" accept=".pdf,.doc,.docx,.xls,.xlsx,.png,.jpg,.jpeg,.webp,.zip" disabled={busy} onChange={e=>{const files=Array.from(e.target.files??[]);if(files.length)onUpload(files);e.currentTarget.value=''}}/></label></div>{!vault.files?.length?<EmptyState message={ar?'لا توجد ملفات مرفوعة.':'No uploaded files.'}/>:<div className="divide-y divide-slate-100">{vault.files.map(file=><div key={file.id} className="flex items-center justify-between gap-3 py-3"><div className="flex min-w-0 items-center gap-3"><span className="rounded-lg bg-slate-100 p-2"><FileText className="h-4 w-4 text-slate-500"/></span><div className="min-w-0"><p className="truncate text-xs font-black text-slate-800">{file.original_name}</p><p className="text-[10px] text-slate-400">{formatBytes(file.file_size,ar)}</p></div></div><div className="flex gap-1"><a href={apiUrl(`/confidential-financial-vaults/${vault.id}/files/${file.id}/download`)} className="rounded-lg p-2 text-teal-700 hover:bg-teal-50" aria-label={ar?'تنزيل':'Download'}><ArrowDownToLine className="h-4 w-4"/></a><button onClick={()=>onRemove(file.id)} className="rounded-lg p-2 text-slate-400 hover:bg-rose-50 hover:text-rose-600" aria-label={ar?'إزالة':'Remove'}><Trash2 className="h-4 w-4"/></button></div></div>)}</div>}</article></div></section>}

function PermanentQr({value,active,ar}:{value:string;active:boolean;ar:boolean}){const canvas=useRef<HTMLCanvasElement>(null);const [copied,setCopied]=useState(false);useEffect(()=>{if(canvas.current)QRCode.toCanvas(canvas.current,value,{width:220,margin:2,color:{dark:'#143f3c',light:'#ffffff'}})},[value]);const download=()=>{if(!canvas.current)return;const a=document.createElement('a');a.download='confidential-financial-qr.png';a.href=canvas.current.toDataURL('image/png');a.click()};return <article className="rounded-2xl border border-slate-200 bg-white p-4 text-center shadow-sm"><h3 className="flex items-center justify-center gap-2 text-sm font-black"><QrCode className="h-5 w-5 text-teal-700"/>{ar?'رمز QR الدائم':'Permanent QR'}</h3><div className={`mx-auto mt-3 w-fit rounded-xl border p-2 ${active?'':'opacity-40'}`}><canvas ref={canvas}/></div><p className="mt-3 break-all rounded-lg bg-slate-50 p-2 text-[9px] text-slate-500">{value}</p><div className="mt-3 grid grid-cols-2 gap-2"><Button size="sm" variant="outline" onClick={async()=>{await navigator.clipboard.writeText(value);setCopied(true);setTimeout(()=>setCopied(false),1500)}}><Copy className="me-1 h-3.5 w-3.5"/>{copied?(ar?'تم النسخ':'Copied'):(ar?'نسخ الرابط':'Copy')}</Button><Button size="sm" onClick={download}><ArrowDownToLine className="me-1 h-3.5 w-3.5"/>{ar?'تنزيل QR':'Download'}</Button><a href={value} target="_blank" rel="noreferrer" className="col-span-2 inline-flex h-9 items-center justify-center rounded-lg bg-teal-50 text-xs font-bold text-teal-800"><ExternalLink className="me-1.5 h-4 w-4"/>{ar?'فتح الصفحة المحمية':'Open protected page'}</a></div><p className="mt-3 rounded-lg bg-emerald-50 p-2 text-start text-[10px] font-bold leading-5 text-emerald-800">{ar?'الرابط لا ينتهي ولا يتغير. يمكن تغيير كلمة المرور أو تعطيل الوصول دون إعادة طباعة QR.':'The link never expires or changes. Change the password or disable access without reprinting the QR.'}</p></article>}

function VaultForm({open,close,ar,form,setForm,editing,save,busy}:{open:boolean;close:()=>void;ar:boolean;form:typeof emptyForm;setForm:(value:typeof emptyForm)=>void;editing:boolean;save:()=>void;busy:boolean}){return <Modal isOpen={open} onClose={close} title={editing?(ar?'تعديل الخزنة':'Edit vault'):(ar?'خزنة مالية جديدة':'New financial vault')} maxWidth="lg"><form onSubmit={(e:FormEvent)=>{e.preventDefault();save()}} className="space-y-4"><label><span className="mb-1 block text-xs font-black text-slate-600">{ar?'اسم داخلي للخزنة':'Internal vault name'}</span><input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className={input}/></label><label><span className="mb-1 block text-xs font-black text-slate-600">{ar?'وصف داخلي اختياري':'Optional internal description'}</span><textarea rows={2} value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className={`${input} h-auto py-3`}/></label><div className="grid gap-3 sm:grid-cols-2"><label><span className="mb-1 block text-xs font-black text-slate-600">{editing?(ar?'كلمة مرور جديدة (اختياري)':'New password (optional)'):(ar?'كلمة المرور':'Password')}</span><input type="password" required={!editing} minLength={12} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} autoComplete="new-password" className={input}/></label><label><span className="mb-1 block text-xs font-black text-slate-600">{ar?'تأكيد كلمة المرور':'Confirm password'}</span><input type="password" required={!editing||Boolean(form.password)} minLength={12} value={form.password_confirmation} onChange={e=>setForm({...form,password_confirmation:e.target.value})} autoComplete="new-password" className={input}/></label></div><p className="flex items-start gap-2 rounded-xl bg-slate-50 p-3 text-[10px] font-bold leading-5 text-slate-600"><KeyRound className="mt-0.5 h-4 w-4 shrink-0 text-teal-700"/>{ar?'12 حرفاً على الأقل، وتتضمن حرفاً كبيراً وصغيراً ورقماً ورمزاً. تغييرها يلغي الجلسات المفتوحة ولا يغير QR.':'At least 12 characters with upper and lower case, a number, and a symbol. Changing it revokes sessions without changing the QR.'}</p><label className="flex items-center gap-2 text-xs font-bold text-slate-700"><input type="checkbox" checked={form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/>{ar?'تفعيل الوصول من QR':'Enable QR access'}</label><div className="flex justify-end gap-2 border-t pt-4"><Button type="button" variant="outline" onClick={close}>{ar?'إلغاء':'Cancel'}</Button><Button type="submit" isLoading={busy}><LockKeyhole className="me-2 h-4 w-4"/>{ar?'حفظ الخزنة':'Save vault'}</Button></div></form></Modal>}
function formatBytes(value:number,ar:boolean){if(value<1024)return `${value} ${ar?'بايت':'B'}`;if(value<1024*1024)return `${Math.round(value/1024)} ${ar?'ك.ب':'KB'}`;return `${(value/1024/1024).toFixed(1)} ${ar?'م.ب':'MB'}`}
